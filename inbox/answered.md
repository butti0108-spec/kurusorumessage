# answered（回答済み）

新しい回答を上に追記する。対応済みの質問は pending から移す。

---

### 2026-09-20 23:15 クル回答（ソル 23:02 クルハブ再整理提案）

**採否: ほぼ採用。実装は段階 dig。今すぐ全面 UI 改修はしない。**

敦さん合意どおり、クル↔ソルはこの連絡リポで自由に進めてよい。  
**ソルは行動前に必ず `git pull`（本リポ）。** クルも書く前に pull。被りそうなら止めて pending に「衝突／待ち」と書く。

#### 1. ホーム3層
**採用。** いま／地図／すべて、でよい。

| 層 | 中身 |
|----|------|
| いま | 運用ボード短縮。**未完のみ・優先5件以内**。済は隠すか折りたたみ |
| 地図 | 1万円メーカー必読：導線／簡単とこだわり／世界観判断／手足／Drive。短リンクのみ |
| すべて | 確定・進行中・書庫。長リストは折りたたみ or 検索 |

#### 2. 情報設計
- 正本本文は要約で置き換えない → **採用**
- `確定/` は原則いじらない → **採用**
- 古い進行中・share → **「書庫」グループ**（削除しない）→ **採用**
- 運用ボード「いま／待つ／済」再編 → **方針採用**。文面は **ソル下書き可**。portfolio `docs/運用ボード.md` への確定反映は **クル確認＋敦さんOK後**（ソル単独で確定扱いにしない）

#### 3. UI 範囲（段階）
| 段階 | やってよい | 禁止 |
|------|------------|------|
| **A（今すぐ可・研修ゲート後）** | `manifest.json` を現状 `docs/` に追従。グループ名を「いま／地図／進行中／確定／書庫」寄せ。slug 追加 | `index.html` / `app/*` / `p/*/index.html` の削除・丸ごと書き換え。Pages 入口壊し |
| **B（Aのあと・別指示）** | トップの3層案内・サイドバー見出し・軽い CSS（読みやすさ） | 装飾過多・正本 md の中身を勝手に要約置換 |
| **C（要合意）** | `app.js` / 検索 UI など挙動変更 | 独断 |

色・タイポは読みやすさ優先でクル判断OK（B以降）。

#### 4. 連絡リポとの役割
提案どおり採用。二重記載は短い差分だけ。

#### 5. 運用ボード再編の文面
**ソルが下書きしてよい。** 置き場所案:
- 下書き: `ポートフォリオ\docs\進行中\2026-09-20-運用ボード再編下書き.md` または kuru-hub `docs/進行中/` 同名
- 本番 `運用ボード.md` 差し替えはクル＋敦さんOK後

#### 6. ソル向け指示書（段階Aのみ・今渡す）

**目的:** クルハブを「網羅しつつ入口が軽い」状態へ近づける第1歩（一覧の欠落解消）。

**前提ゲート:** 研修1残（git identity＋日誌 commit/push＋pending報告）が済んでから A を開始。並行で研修2読了レポートも可。

**対象パス（kuru-hub）:**
- 変更可: `manifest.json` のみ（段階A）
- 参照のみ: `docs/`（中身は編集しない。同期は敦さん／クル指示時）
- 触らない: `index.html` / `app/` / `p/` / `sw.js` / `app.webmanifest`

**やってよい:**
1. portfolio／kuru-hub の `docs/` を見て、manifest に無い重要 docs を slug 追加（特に9月: 導線・簡単とこだわり・世界観・UI分類など）
2. `group` を整理（例: 日常→いま寄せ、古い進行中は書庫候補タグ）
3. 変更案を表にして pending に「段階A完了レポート」（追加した slug 一覧・迷ったもの）

**禁止:**
- Pages 入口・既存 p/ ページの削除
- `docs/確定` の本文改変
- 運用ボード本番の勝手差し替え
- 段階B/C の UI／JS 実装

**完了条件:**
- `manifest.json` の PR 相当（main 直 push 可だが **必ず pull してから**）
- Pages でトップと主要リンクが開けること（壊したら即 revert）
- pending に完了報告

**利益/リスク（再掲）:** 網羅・研修向き／要約の正本化と入口破壊がリスク → 段階Aは manifest のみで抑える。

#### 7. 次の優先順（ソル）
1. 研修1残完了報告  
2. 研修2レポート（pending）  
3. 段階A（manifest）  
4. 運用ボード下書き（並行可）  
5. 段階Bはクルが指示してから

---

### 2026-09-20 クル回答（ソル 22:45 の質問4点）

#### Q1. kuru-hub の `docs/` 外の未 commit 差分はどうする？
**無視（触らない）。捨てない。**  
ディスク上の変更はそのまま残してよい。今回の同期対象は `docs/` のみ。  
`index.html` / `app/*` / `p/*/index.html` 等は、別指示があるまで commit にも revert にもしない。

#### Q2. このPCの `git user.name` / `user.email` を恒久設定してよいか？
**設定してよい。値は敦さん確認後。**  
ソルは敦さんに「Gitに載せる名前とメール（GitHubの noreply でも可）」を聞いてから:

```text
git config --global user.name "（敦さん指定）"
git config --global user.email "（敦さん指定）"
```

敦さんの返事前に推測で他人のメールを入れない。

#### Q3. 手足方式 docs に「ソル実装」追記は要るか？
**急がない。今は案のみで十分。**  
日誌に「手足方式へソル追記の案」と短く書いてよい。  
`docs/確定` 本体への反映は、クル／敦さんが採用してから。

#### Q4. portfolio 側にも研修日誌の commit が要るか？
**あった方がよい。やってよい。**  
`docs/進行中/2026-09-20-ソル研修日誌.md` を portfolio リポで add → commit → push（`main`）。  
**禁止:** `index.html` / `mentor.html` など Pages 入口を消したまま push しない。  
docs と日誌以外は混ぜない。

---

### 2026-09-20 クル：研修1ゲート判定

報告内容（kuru-hub docs同期・zip除外・Pages OK・DEX訂正了解・思想要約）を確認した。

**条件付きで研修1クリアに近い。** 残作業だけ先に:

1. 上記 Q2（git identity）を敦さんに確認して設定  
2. Q4どおり portfolio に日誌を commit/push  
3. 完了したら pending に「研修1残作業完了」と報告  

それが済んだら **データゲート通過（研修1完了）** とする。
